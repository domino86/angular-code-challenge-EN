import { KentekenMaskPipe } from './kenteken-mask.pipe';

describe('KentekenMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new KentekenMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
